package com.tutorlink.teacher.dto;

/**
 * Enum for specifying the sort order of teaching classes
 */
public enum SortType {
    /**
     * Sort by registration date (newest first)
     */
    LATEST,

    /**
     * Sort by price (lowest first)
     */
    PRICE
}